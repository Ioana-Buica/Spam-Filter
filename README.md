var_1: Spam Filter
This project implements an anti-spam filter that preprocesses email data, builds a vocabulary, and trains a Naive Bayes model for spam classification.



var_2: Spam Filter with TF-IDF and SVC
This contains a Python-based spam filter that uses TF-IDF for text vectorization and a Support Vector Classifier (SVC) for classification. 
